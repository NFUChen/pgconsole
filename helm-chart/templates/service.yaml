apiVersion: v1
kind: Service
metadata:
  name: {{ include "pgconsole.fullname" . }}
  labels:
    {{- include "pgconsole.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pgconsole.selectorLabels" . | nindent 4 }}
